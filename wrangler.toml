name = "hot-news"
main = "worker.js"
compatibility_date = "2025-01-01"

[assets]
directory = "./public"
binding = "ASSETS"

# 数据不再走 KV：前端直接读阿里云 OSS（国内直连），Actions 每 30 分钟上传 data.json 到 OSS。
# 由此彻底绕开 Cloudflare 出境 fetch 到 GitHub/jsDelivr 不可达的问题。无需 kv_namespaces / cron。
