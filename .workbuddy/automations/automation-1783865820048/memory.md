# 热闻聚合 · 每小时采集

## 2026-07-12 23:12 (第 1783865820048 次)
- 采集成功：9 个源，277 条（exit 0，data.json ~123KB）。
- 未出现 "[oss] 已上传"：collector.py 主流程已改为只写本地 data.json + 可选 Supabase（见 collector.py:422 注释「不再写入 OSS，避免污染 Obsidian 同步桶」），OSS 上传段 `oss_flush()` 仍在但不再被调用。故该判定为代码设计使然，非偶发失败。重试一次结果相同。
- 结论：采集正常，但原任务对 "[oss] 已上传" 的预期与当前代码不符，需确认是否应恢复 OSS 上传或更新任务判定条件。本次未改代码。

## 2026-07-13 00:11 (第 1783865820048 次)
- 第 1 次：9 源 277 条，exit 0，data.json 已写（126232 bytes，updated_at 00:13:39）；仍无 "[oss] 已上传"（代码设计使然，OSS 上传段不再被调用）。
- 第 2 次（重试）：进程被 SIGTERM 提前终止，stdout 为空——疑似摘要生成网络读超时引发上层中断，属瞬时失败；data.json 已由第 1 次落盘，数据完整（277 条）。
- 结论：采集与本地落盘均正常；"[oss] 已上传" 判定因代码设计无法满足。未改代码。建议更新任务判定条件或恢复 OSS 上传。
